#!/bin/bash
# make aliases accessible by shell scripts

# environment variables need to be loaded first
#set envvars
source "$MODULAR_BASHING_BIN_DIR/setup.envvars"

# set aliases
source "$MODULAR_BASHING_BIN_DIR/setup.aliases"

#set autocomplete
source "$MODULAR_BASHING_BIN_DIR/setup.autocomplete"
